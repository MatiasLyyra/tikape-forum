<h1><%= @discussion.title %></h1>
<% @messages.each do |message| %>
  <div class="panel panel-default">
    <div class="panel-heading">
      <h3 class="panel-title">By: <%= User.GetUserById(message.user_id).try(:nick) %> at <%= message.time.strftime('%H:%M') %></h3>
    </div>
    <div class="panel-body">
      <%= message.message %>
    </div>
  </div>
<% end %>

<%= form_tag category_discussion_messages_path(@category, @discussion) do |f| %>
<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">New Message</h3>
  </div>
  <div class="panel-body">
    <%= text_area_tag 'message', nil, class:'form-control', placeholder:'Kirjoita viesti', rows:'10' %>
    <br>
     <%= submit_tag "Post Message", class: "btn btn-large btn-primary" %>
  </div>
</div>
<% end %>
